import { cdnBaseURL } from '@constants/config';
const LogoFull = () => {
  return (
    <img alt="behtarino-full-logo" width="128px" src={`${cdnBaseURL}/behtarino-logo-full.png`} />
  );
};

export default LogoFull;
